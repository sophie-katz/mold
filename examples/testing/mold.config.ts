import { defineConfiguration } from '../../dist/index';

export default defineConfiguration({
  name: 'testing',
  templateVersion: '0.1.0',
  moldVersion: '0.1.0',
  templateOptions: {
    sourcePath: 'template',
  },
});
